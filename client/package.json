{
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "dotnet fable",
        "bundle": "esbuild App.fs.js --bundle --minify --outfile=../public/bundle.js"
    },
    "devDependencies": {
        "esbuild": "0.14.34"
    }
}
